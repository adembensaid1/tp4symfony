{% extends 'base.html.twig' %}
{% block title%} Liste des Articles{% endblock %}
{% block body %}
    {{ include('inc/navbar.html.twig')}}
    {% if articles %}
    <table id="articles" class="table table-striped">
    <thead>
    <tr>
    <th>id</th>
    <th>Nom</th>
    <th>Prix</th>
    </tr>
    </thead>
    <tbody>
    {% for article in articles %}
    <tr>
    <td>{{ article.id }}</td>
    <td>{{ article.nom }}</td>
    <td>{{ article.prix  }}</td>

    <td>
    <a href="/show/{{ article.id }}" class="btn btn-dark">Détails</a>
    <a href="/modifier/{{ article.id }}" class="btn btn-dark">Modifier</a>
    <a href="/delete/{{ article.id }}" class="btn btn-danger" onclick="return confirm('Etesvous sûr de supprimer cet article?');">Supprimer</a>
    </td>
    </tr>
    {% endfor %}
    </tbody>
    </table>
    {% else %}
   Atelier framework Web côté serveur Symfony 4
   Nadhem Bel Hadj nadhemb@yahoo.com AU : 2019/2020
    <p>Aucun articles</p>
    {% endif %}
   
{% endblock %}